# Human UAT summary (template)

Fill after ≥3 validated participant result JSON files, or generate with:

```bash
node scripts/human-uat-summarize.mjs --out summary.md result-*.json
```

- Kit version:
- Date:
- Participants (anonymized roles only):
- Package identity (npm / packed-tarball + version):
- Success threshold: ≥3 independent roles; all UAT-HUMAN-1..8 completed; no mistaken trust; no abandonment
- Tasks passed / failed:
- Trust comprehension misses:
- Installation / auth / docs friction (separate from analysis quality):
- Linked blocker issues:
- Decision: pass / fail for milestone gate
